apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
  labels:
    app: telemedicine-service
spec:
  type: ClusterIP
  selector:
    app: telemedicine-service
  ports:
    - name: http
      protocol: TCP
      port: 8087
      targetPort: 8087
